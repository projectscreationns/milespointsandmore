# NOTE: public/ (the built site) IS committed so Cloudflare can deploy it directly
# without needing Python in its build environment. `python build.py` regenerates it;
# the weekly automation rebuilds and commits it automatically.

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.log

# Local preview helper (machine-specific paths)
.claude/launch.json
